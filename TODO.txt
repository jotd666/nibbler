
- at start nibbler is not shown (head/tail in blue): rip tiles when nibbler appears
  then create a char table flag head/tail chars. Enable direct char display only when the
  game displays nibbler at start, else don't display anything during game

- RE wriggling tail: using frame_index_e9 for 8 frames
  => we should build a table of frames matching frame_index_e9 in each direction
- tail seems to continue straight after it has turned: corner chars 50-53 can help
  depending on direction, if corner char is following, then draw blank char after (draw 0 first to check)
  & redraw corner char too
- eliminate unused cluts for nibbler body
- optim: remove copy char data when moving nibbler
- bg: allocate colors manually: white is yellow now
  maybe use copper if possible?
- rip F sound
- directly restarts from level 1 when all lives lost
 => breakpoint on game start
- cheat add 10000 to score 
- A8DD invalid mem access after level 34 expected?


- cheat infinite time

- set DSW difficulty easy/hard
- DSW lives 3,4,5,6
- highscore load/save
- set dip switch free play
- detect not game in play for start up/down
  but maybe not needed

trace interrupt status:
trace mame.tr,,,{tracelog "P=%02X ",p&4}


charset decoding: 1 byte = 1 column of 8 pixels
first bitplane: 0x1000+(8*charcode), second bitplane 0x1800+

tag [copy_charset_data_loop=nb_chars,mirrored]
nb_chars = 9 (head)
src_ptr=$31
dst_ptr=$1b

nb_chars = 1: (body)
src_ptr=$17
dst_ptr=$19

parse tag, generate ptr load & call
; d0 < nb_chars
; d1.w < charset ROM address
osd_change_char_data

atm only called with 3D and 40
40: going up
3D: going left
3F: going down
3E: going right

9: 31 or 61 at corners

completely missing tail char copy that seems
of variable length! see at A600

tail: 4D 49 4B
